/**
 * BHOOMITRACE_CONFIG
 * Optional configuration. Leave GOOGLE_MAPS_API_KEY empty to use
 * key-free "Open in Google Maps" links (default, always works).
 * Fill it in ONLY with a key that has an HTTP-referrer restriction
 * limited to your own GitHub Pages domain — see README section 6.
 * NEVER commit an unrestricted key to a public repository.
 */
window.BHOOMITRACE_CONFIG = {
  GOOGLE_MAPS_API_KEY: '' // e.g. 'AIza...' — restricted to your domain only
};
